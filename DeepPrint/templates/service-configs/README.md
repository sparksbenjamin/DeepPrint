# Service Config Placeholders

Drop service-specific configuration fragments here if a persona needs to swap
base files before deployment. `deepprint.py` already supports post-start
container injection through `files_to_inject`.
